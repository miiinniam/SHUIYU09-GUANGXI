
**完整应用说明 + 开发代码架构与详细逻辑

### 一、应用完整说明

**应用名称**：水鱼发牌神器（广西水鱼酒桌专用发牌助手）  
**定位**：极简、酒桌实用的发牌辅助工具，仅负责**公平共享发牌 + 自动算牌 + 大屏显示结果**，不做对战、不抢注意力。  
**核心亮点**：  
- 同一个房间**共用一副真实牌堆**，彻底避免多人发重牌。  
- **牌面显示严格分为三个区域**（上面头牌、下面尾牌、底部结果栏）。  
- 底部结果栏**对子统一只显示“保”**。  
- 支持盖牌模式、变牌模式、记酒器、换庄等酒桌必备功能。

#### 1.1 首页（第一页）
- 两个大按钮：
  - **选项一：创建房间**  
    输入/自动生成**四位数房间号** → 进入初始设定：  
    - 开关：**是否加入变牌**（大小王+花牌模式）  
      - **不加变牌**（默认）：标准52张牌。水鱼出现时**不自动换庄**，系统记录本副牌水鱼次数，剩余牌<8张时提示“本副牌即将结束”。  
      - **加变牌**：包含大小王。水鱼出现时**自动随机换庄 + 重新洗牌**。  
    - 人数（4-8人）。  
    创建后云端立即生成**共享牌堆**。
  - **选项二：加入房间**  
    输入四位数房间号 + 设置自己昵称 → 加入并同步共享牌堆状态。

#### 1.2 游戏页面（核心界面）
- **顶部固定栏**（始终可见）：
  - 自己昵称 + 角色（例：“老王（庄家）” / “小李（闲家）”）。
  - 庄家左上角：**换庄**按钮（点击选人，换庄后自动重新洗牌共享牌堆）。
  - 右上角：**记酒器**（酒杯图标 + 数字，点击+1/-1或手动备注欠杯数，全房间实时同步）。

- **牌面显示区域**（严格分为**三个区域**，酒桌大字体、高对比度、横竖屏自适应）：
  1. **上面区域（上半屏幕 ≈55%）**：**头牌（头铺）** — 两张超大牌面（横排，占屏宽40%以上），支持轻点放大、拖拽摆牌、盖牌/反牌。
  2. **下面区域（下半屏幕 ≈30%）**：**尾牌（尾铺）** — 两张超大牌面（同风格）。
  3. **底部固定栏（≈15%高度，始终固定）**：结果显示区（详细见下方）。

- **中间临时区**（发牌后出现）：4张牌平铺，支持手指**拖拽**任意调整到头牌/尾牌区域，反复修改。

- **底部固定栏（最新要求，精确实现）**：
  - **左侧大字**：头铺结果  
    - 对子（保） → 只显示 **“保”**。  
    - 非对子 → 显示两张点数总和取个位（例：“5点”、“0点”、“9点”）。
  - **右侧大字**：尾铺结果（规则完全一致：对子只显示“保”，非对子显示点数）。
  - **中间/整体高亮**：若头铺和尾铺**都是保** → 立即突出显示 **“水鱼！”**（金色大字 + 轻微跳动/闪烁动画1-2秒）。
  - 盖牌模式下：底部暂不显示具体结果，只显示“已盖牌，等待揭晓”。

- **底部操作按钮**：
  - 左下角：红色“退出”键（二次确认）。
  - 右下角：绿色大按钮“重新发牌 / 下一局”（触发共享牌堆新发牌）。

- **盖牌模式**：
  - 发牌后**默认全桌统一盖牌**（只显示牌背）。
  - 玩家点击牌面/区域 → 翻开查看真实点数 → 再次点击 → 重新盖牌。
  - 确认头尾后，上面头牌区、下面尾牌区保持盖牌状态，底部结果栏暂不显示具体数字，等待庄家或玩家统一揭晓。

#### 1.3 共享牌堆与规则
- **同一个房间共用一副牌**：房间创建时云端生成并锁定一副牌堆（52或54张）。每次发牌从**剩余共享牌堆**中一次性抽取所有人需要的牌（人数×4张），分别推送给对应玩家手机，彻底防重牌。
- **点数计算**（严格广西传统）：
  - A=1，2-9=面值，10/J/Q/K=0。
  - 每组两张：点数相同 → “保”；不同 → 相加取个位。
  - 双保 → “水鱼！”（至尊水鱼额外提示）。
- **不加变牌**：继续用剩余共享牌堆，耗尽时提示统计。
- **加变牌**：大小王摆牌时可选择变成任意点数，水鱼触发自动换庄+新共享牌堆。

### 二、开发代码架构与详细逻辑

#### 2.1 技术栈
- **首选**：微信小程序 + 微信云开发（CloudBase）—— 支持实时共享牌堆同步。
- **备选**：Flutter + Firebase Realtime Database。
- 仅多人房间需要网络

#### 2.2 整体架构
```
pages/
├── index/          ← 首页（创建/加入）
├── setting/        ← 房间初始设定
├── game/           ← 游戏主页面（三个区域核心）
components/
├── card/           ← 单张牌组件（拖拽、盖牌/反牌）
├── bottomBar/      ← 底部固定栏（最新“保”简化逻辑）
├── drinkCounter/   ← 记酒器
utils/
├── deck.js         ← 牌堆引擎（共享牌堆）
├── calculator.js   ← 点数计算引擎（新增只显示“保”）
├── rules.js        ← 规则配置
services/
├── room.js         ← 房间管理
├── cloud.js        ← 云端共享牌堆API（核心）
├── storage.js      ← 本地缓存
app.js
```

#### 2.3 关键模块详细逻辑（伪代码）

**1. 云端共享牌堆（cloud.js）**
```javascript
async function createSharedDeck(roomId, isVariant) { ... }  // 同前
async function dealFromSharedDeck(roomId, playerCount) { ... }  // 从剩余牌堆抽取，推送myCards
```

**2. 点数计算引擎（calculator.js —— 已按最新要求修改）**
```javascript
function calculateGroup(cards) {  // cards: 2张牌
  // 处理大小王变牌...
  const p1 = cards[0].point;
  const p2 = cards[1].point;
  
  if (p1 === p2) {
    return { type: 'bao', display: '保' };  // ← 关键修改：只显示“保”，不再带X
  }
  
  let sum = (p1 + p2) % 10;
  return { type: 'point', display: `${sum}点` };
}

function calculateShuiyu(hand) {  // hand = {head: [2张], tail: [2张]}
  const headRes = calculateGroup(hand.head);
  const tailRes = calculateGroup(hand.tail);
  
  const isShuiyu = headRes.type === 'bao' && tailRes.type === 'bao';
  
  return {
    head: headRes,
    tail: tailRes,
    isShuiyu: isShuiyu,
    displayBottom: {
      left: headRes.display,      // “保” 或 “5点”
      right: tailRes.display,     // “保” 或 “0点”
      center: isShuiyu ? '水鱼！' : ''
    }
  };
}
```

**3. 游戏页面数据流（game.js）**
- `data: { headCards: [], tailCards: [], bottomDisplay: {left:'', right:'', center:''}, isCovered: true }`
- 拖拽确认后 → 调用 `calculateShuiyu` → 实时更新底部固定栏。
- 盖牌状态变化 → 动态控制上面/下面区域显示牌背或牌面，底部智能隐藏/显示结果。

**4. 换庄 & 牌堆重置**
```javascript
async function switchDealer(roomId, newDealerId) {
  // 更新庄家 + 重新洗共享牌堆
}
```

**5. 底部固定栏组件（bottomBar）**
- 接收 `displayBottom` 对象。
- 左侧 `<text class="big left"> {{bottomDisplay.left}} </text>`（“保”或点数）。
- 右侧同理。
- 中间条件渲染：`isShuiyu ? <text class="shuiyu">水鱼！</text> : null`。
